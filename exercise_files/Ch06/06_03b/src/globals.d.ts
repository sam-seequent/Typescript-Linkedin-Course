// This file declares global functions that can be used throughout the application (Don't need to include definitions)
declare global {
    // Formats a date into human-readable string
    function formateDate(date: Date): string;
}

export {};